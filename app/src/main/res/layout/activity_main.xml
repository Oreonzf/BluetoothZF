<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp">

    <!-- Botão para ligar o Bluetooth -->
    <Button
        android:id="@+id/btn_enable_bluetooth"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Ligar Bluetooth"
        android:layout_marginBottom="8dp" />

    <!-- Botão para iniciar a descoberta -->
    <Button
        android:id="@+id/btn_discover_devices"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Descobrir Dispositivos"
        android:layout_marginBottom="8dp" />

    <!-- RecyclerView para exibir dispositivos -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:scrollbars="vertical" />

</LinearLayout>
